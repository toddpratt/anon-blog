apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: anon-blog-cert
  namespace: anon-blog-test
spec:
  secretName: anon-blog-cert-secret
  issuerRef:
    name: letsencrypt-dns
    kind: ClusterIssuer
  dnsNames:
  - abapi-test.vengarl.com
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: anon-blog
  namespace: anon-blog-test
spec:
  entryPoints:
  - websecure
  tls:
    secretName: anon-blog-cert-secret
  routes:
  - match: Host(`abapi-test.vengarl.com`)
    kind: Rule
    services:
    - name: anon-blog
      port: 80
